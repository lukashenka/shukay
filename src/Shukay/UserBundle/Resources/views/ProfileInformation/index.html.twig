{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ProfileInformation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Avatar</th>
                <th>Firstname</th>
                <th>Lastname</th>
                <th>Rate</th>
                <th>Description</th>
                <th>Contacts</th>
                <th>About</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('profile_setting__show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.avatar }}</td>
                <td>{{ entity.firstName }}</td>
                <td>{{ entity.lastName }}</td>
                <td>{{ entity.rate }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.contacts }}</td>
                <td>{{ entity.about }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('profile_setting__show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('profile_setting__edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('profile_setting__new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
